function mcomp_bms(model_dirs)



n_models = numel(model_dirs);

% get model files
for m = 1:n_models
    mfiles{m} = cellstr(ls(fullfile(model_dirs{m},'*mat')));
    n_subjects(m) = numel(mfiles{m});
end

F = NaN(max(n_subjects),n_models);


% Load models
for m = 1:n_models
    load(m1_files{
    
end